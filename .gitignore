# Python
__pycache__/
*.pyc
*.egg-info/
dist/
.venv/
.pytest_cache/
.ruff_cache/

# devenv / direnv
.devenv/
.devenv.flake.nix
.direnv/

# WSL / Windows download metadata
*:Zone.Identifier

# Test media
*.mp4
*.avi

# Dashboard event log (runtime artifact)
*.db

# Saved live-tuning overrides (runtime artifact; config/default.yaml stays canonical)
config/tuning.local.yaml

# ML model weights (fetch with scripts/download_models.py)
models/
